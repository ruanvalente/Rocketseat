// Intervalo e timeout com elas podemos definir um intervalo de tempo que um evento irá acontecer.

function showMe() {
  console.log( 'Hello word' );
}

// setInterval( showMe, 1000 );

setTimeout( showMe, 1000 );